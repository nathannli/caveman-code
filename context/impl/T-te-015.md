Build site: context/plans/build-site-token-efficiency.md
# T-te-015: CaveKit PRESET_MODELS folded into routing profile
**Status:** done

`packages/cavekit-extension/routing/cavekit-profile.json` — single `RoutingProfile` exposing:
- plan/edit/explore/verify role tiers
- `cavekitPhaseRole` mapping `draft|architect→plan`, `build→edit`, `inspect→verify`
- `cheapTier` for cost-aware downgrade

`DefaultModelRouter` honors `cavekitPhaseRole` via one lookup (R7 "single router lookup" semantics satisfied — test: "honors CaveKit phase → role folding").

Acceptance R7 AC-1, AC-3 met. AC-2 (CaveKit phase mappings identical under new router) pending live cavekit-extension regression test in Tier 1.
