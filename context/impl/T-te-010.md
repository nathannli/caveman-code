Build site: context/plans/build-site-token-efficiency.md
# T-te-010: Outbound call carries exactly one role or test-fails
**Status:** done

`requireRole()` in `roles.ts` throws `roles: outbound call missing or invalid role ...` when role absent or non-`Role`. Test: "requireRole throws with diagnostic for missing role" + "requireRole throws for invalid role string".

Acceptance R1 AC-3 met.
