# T-016: Cave Mode Status Line and Earendil Announcement Removal
**Status:** done

Implemented.

Files changed
- `packages/coding-agent/src/modes/interactive/interactive-mode.ts`
- removed `packages/coding-agent/src/modes/interactive/components/earendil-announcement.ts`

What changed
- Startup header now checks effective startup cave mode via `this.session.getCaveModeSessionState()`
- Cave mode line renders only when enabled
- Cave mode line text contains both `cave mode` and `compression`
- Cave mode line uses dimmed accent styling via `chalk.dim(theme.fg("accent", ...))`
- Earendil announcement component file removed
- No startup path import/render remains for Earendil banner

Acceptance criteria
- R4/AC-1: met
- R4/AC-2: met
- R4/AC-3: met
- R5/AC-1: met
- R5/AC-2: met
- R5/AC-3: met

Validation
- `tsgo --noEmit` passed
- `npm run check` failed on unrelated pre-existing browser smoke issue:
  - `scripts/browser-smoke-entry.ts:1:35 Could not resolve "@cavepi/pi-ai"`

Notes
- Current cave mode status line shows effective compression level:
  - `cave mode: active | compression: <lite|full|ultra>`
- `rg -n "EarendilAnnouncementComponent|earendil-announcement" packages/coding-agent/src -S` returns no matches
