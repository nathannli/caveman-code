Build site: context/plans/build-site-token-efficiency.md
# T-te-098: Return ambiguous candidate list when name matches multiple symbols
**Status:** done
When `findSymbolRange()` yields more than one match, `editSymbol()` returns `{ status: "ambiguous", reason: "ambiguous", candidates: [{file, name, line, signature}] }`. Caller can then disambiguate via file:line.
Test: "returns ambiguous with all candidates when symbol name matches multiple locations".
Acceptance R3 AC-1, AC-2, AC-3 met.
