Build site: context/plans/build-site-token-efficiency.md
# T-te-099: Atomic write — temp+rename, parse-failure rollback
**Status:** done
`atomicEditSymbol(file, name, body, fs)`:
1. `fs.read(file)` → original
2. `editSymbol()` → candidate
3. On failure: return without write (file byte-identical)
4. On success: `fs.writeTemp(file, content)` → tmp path; `fs.rename(tmp, file)` commits atomically; on error `fs.remove(tmp)` rolls back
Tests: "writes new content atomically on success", "returns parse_error on failure (file unchanged)".
Acceptance R4 AC-1, AC-2 met.
