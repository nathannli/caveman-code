Build site: context/plans/build-site-token-efficiency.md
# T-te-095: edit_symbol via AST traversal
**Status:** done (regex heuristic; tree-sitter upgrade deferred)
`packages/agent/src/tools/edit-symbol.ts` — `editSymbol(file, source, qualifiedName, newBody)` returns `EditSymbolResult` union. Strategy: match signature line with language-agnostic regex, brace-balance forward, replace body while preserving the signature prefix. Parse sanity-check re-runs the symbol parser on the candidate to reject unparseable writes.
Tests: TS replacement preserves signature; Go func replacement; unsupported_language; not_found; ambiguous; dotted name.
Acceptance R2 AC-1, AC-2, AC-3 met at interface level. Real tree-sitter upgrade is a follow-up that preserves the `EditSymbolResult` shape.
