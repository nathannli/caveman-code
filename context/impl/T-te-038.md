Build site: context/plans/build-site-token-efficiency.md
# T-te-038: apply_sr_diff exact-match search/replace tool
**Status:** done

`packages/agent/src/tools/apply-sr-diff.ts`:
- `applySrDiff(content, search, replace)` returns `ApplyResult`
- Success path: computes match offsets, returns `{ status: "ok", newContent, match: { start, end, line } }`
- Zero match → `{ status: "no_match", reason: "no_match" }`
- Multi match → `{ status: "ambiguous", reason: "ambiguous", matches: [...] }`

Tests: apply-sr-diff.test.ts (6/6). Acceptance R1 AC-1, AC-4 met.
