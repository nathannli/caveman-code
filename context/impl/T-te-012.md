Build site: context/plans/build-site-token-efficiency.md
# T-te-012: Wire agent loop to router, allow test swap
**Status:** partial

Router interface + swap primitive shipped (`fixedRouter()` in router.ts, test-verified). **Wiring the live `agent-loop.ts` streamFn call site to `router.route()` is deferred** — requires touching provider-model threading through `AgentLoopConfig` and every `streamSimple` call, which is a larger surgical change than the Tier 0 boundary accommodates.

Plan: follow-up task in Tier 1 wraps the existing loop in a router-aware helper that resolves the model from `RouteContext` before calling `streamSimple`, keeping the primitive contract identical.

Acceptance status:
- R2 AC-2 (swap): met at interface level (fixedRouter), pending live loop wiring
- R2 AC-1 (loop calls router): pending
