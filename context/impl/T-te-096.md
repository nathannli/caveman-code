Build site: context/plans/build-site-token-efficiency.md
# T-te-096: Preserve signature line + nested qualified names across 8 languages
**Status:** done
`editSymbol()` extracts `signaturePart = sigLine.slice(0, braceIdx + 1)` and prepends it to the new body. Dotted qualified names resolve by taking the last segment as the target name (e.g. `Foo.method` → `method`). All 8 supported languages share the same regex-heuristic path.
Test: "replaces a TS function body while preserving the signature line", "dotted qualified name resolves last segment".
Acceptance R2 AC-1, AC-3, AC-4 met.
