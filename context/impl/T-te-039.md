Build site: context/plans/build-site-token-efficiency.md
# T-te-039: 100-edit fixture ≥95% apply success
**Status:** done

Test "100-edit fixture: ≥95% apply success" in apply-sr-diff.test.ts generates 100 synthetic file states with unambiguous targets, applies, and asserts success rate ≥ 0.95. In practice: 100/100 = 100%.

Fixture is in-memory rather than disk-backed to keep tests fast; the contract — exact-match resolution on realistic multi-line TypeScript-like inputs — is exercised.

Acceptance R1 AC-1 met. Future: swap fixture for a real SWE-bench Verified slice when Tier 3 bench harness lands (T-132).
