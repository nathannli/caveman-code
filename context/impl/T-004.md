# T-004: Skill bundling, resource discovery, and vanilla Pi compatibility
**Status:** done

Implemented.

Changed:
- `packages/cavekit-extension/src/resources.ts`
- `packages/cavekit-extension/src/hooks/skills-discovery.ts`
- `packages/cavekit-extension/src/__tests__/skills-bundling.test.ts`
- `packages/cavekit-extension/src/__tests__/vanilla-pi.test.ts`
- `packages/cavekit-extension/skills/bundled/*/SKILL.md` (15 bundled skill wrappers)

What changed:
- Added bundled-skill manifest + path helpers in `src/resources.ts`
- Switched `resources_discover` hook to register `skills/bundled/` instead of flat legacy markdown dir
- Added host-compatible `SKILL.md` wrappers for all bundled CaveKit skills
- Added tests for:
  - bundle completeness
  - resource discovery path registration
  - successful host loading via `loadSkills(...)` with zero diagnostics
  - vanilla Pi command safety for all registered `/ck:*` commands

Validation run:
- `cd packages/cavekit-extension && npx vitest --run src/__tests__/skills-bundling.test.ts`
- `cd packages/cavekit-extension && npx vitest --run src/__tests__/vanilla-pi.test.ts`

Results:
- `skills-bundling.test.ts`: passed
- `vanilla-pi.test.ts`: passed

`npm run check`:
- Ran from repo root
- Fails on unrelated existing type errors outside this task:
  - `packages/cavekit-extension/src/rtk-exec.ts`
  - `packages/coding-agent/test/rtk.test.ts`

Acceptance criteria status:

- extension-core/R4
  - AC-1: met
    - 15 bundled CaveKit skills now shipped in host-compatible form under `packages/cavekit-extension/skills/bundled/*/SKILL.md`
  - AC-2: met
    - `resources_discover` returns bundled skill path
    - host `loadSkills(...)` test loads all bundled skills successfully
  - AC-3: met
    - runtime code only resolves/returns paths via `fs.existsSync`; no bundled skill mutation code added

- extension-core/R6
  - AC-1: met
    - `registerSkillsDiscoveryHook()` now registers `getBundledSkillDiscoveryPaths()`
  - AC-2: met
    - discovery-path test verifies bundled skills appear through normal host skill loading

- extension-core/R8
  - AC-1: met
    - vanilla init test passes
  - AC-2: met
    - all registered `/ck:*` commands now exercised for empty-arg safety on vanilla Pi mock
  - AC-3: met
    - vanilla hook/resource tests pass without Cave-specific capability assumptions

Note:
- Left legacy flat markdown skill sources in place
- Compatibility layer uses bundled wrappers for discovery, preserving source content unchanged
