Build site: context/plans/build-site-token-efficiency.md
# T-te-100: Structured parse diagnostic field on write failure
**Status:** done
Parse failure path returns `{ status: "parse_error", reason: "parse_error", diagnostic: string }` with the error message. The diagnostic is a structured field separate from the status/reason so callers can surface it in tool error UIs.
Acceptance R4 AC-3 met.
