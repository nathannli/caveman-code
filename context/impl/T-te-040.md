Build site: context/plans/build-site-token-efficiency.md
# T-te-040: Structured no_match / ambiguous diagnostics
**Status:** done

`ApplyResult` is a discriminated union. `no_match` carries `reason: "no_match"`. `ambiguous` carries `reason: "ambiguous"` plus a `matches: ApplyMatch[]` with `{ start, end, line }` for every hit.

Tests "returns no_match when search string absent" and "returns ambiguous with all matches when search appears multiple times" assert shape + reason.

Acceptance R1 AC-2, AC-3 met.
