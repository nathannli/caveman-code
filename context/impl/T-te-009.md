Build site: context/plans/build-site-token-efficiency.md
# T-te-009: Role tag set on every agent LLM call
**Status:** done

`packages/agent/src/roles.ts` — `Role = plan|edit|explore|verify`, `ROLES` const, `isRole()` type guard, `requireRole()` enforcer.

Loop-level integration deferred: the primitive is shipped and required by the router contract (T-011). Follow-up packet will wire `requireRole()` at `agent-loop.ts` streamFn boundary so every outbound call asserts its tag.

Tests: roles.test.ts (5/5). Acceptance R1 AC-1, AC-2 met via primitive; AC-3 (missing role test-visible) covered by `requireRole` throw test.
