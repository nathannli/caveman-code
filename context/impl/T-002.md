# T-002: Upstream remote tracking and fork sync metadata
**Status:** done

Implemented:

- `.github/upstream-sync.json`
  - machine-readable fork sync contract
  - records `upstream` remote name, fetch URL, disabled push URL, refspec, default branch, sync commands
- `CONTRIBUTING.md`
  - points maintainers to sync metadata
  - documents required local git config
  - adds explicit audit commands for verifying clone state before sync

Verified local repo state:

- `git remote get-url upstream` -> `https://github.com/badlogic/pi-mono.git`
- `git remote get-url --push upstream` -> `DISABLE`
- `git config --get-all remote.upstream.fetch` -> `+refs/heads/*:refs/remotes/upstream/*`
- `git fetch upstream` -> succeeded (`ok fetched (3 new refs)`)

Acceptance criteria met:

- R5 / AC-1: met
  - local repo has remote `upstream` pointing to upstream Pi URL
- R5 / AC-2: met
  - `upstream` push URL is `DISABLE`
- R5 / AC-3: met
  - `git fetch upstream` completed successfully

Notes:

- No `npm run check` run; docs/metadata-only changes
- `.git/config` already satisfied remote requirements, so task implementation focused on explicit, discoverable repo metadata plus maintainer audit guidance
