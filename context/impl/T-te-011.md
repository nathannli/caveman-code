Build site: context/plans/build-site-token-efficiency.md
# T-te-011: ModelRouter interface + DefaultModelRouter
**Status:** done

`packages/agent/src/router.ts`:
- `ModelRouter` interface: `route(ctx) => RoutingDecision`
- `DefaultModelRouter` config-backed via `RoutingProfile`
- `DEFAULT_PROFILE`: plan→opus/long, edit/explore→sonnet/short, verify→haiku/short
- `fixedRouter()` helper for test swap
- CaveKit phase → role folding (feeds T-015)
- Cost-aware downgrade primitive (feeds T-131)

Tests: router.test.ts (6/6). Acceptance R2 AC-1 (interface present), AC-3 (default reads profile) met. AC-2 swap test in same file.
