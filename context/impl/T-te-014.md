Build site: context/plans/build-site-token-efficiency.md
# T-te-014: `--router-profile`, `--plan-model`, `--edit-model` CLI flags
**Status:** done

`packages/coding-agent/src/cli/args.ts`:
- New `Args` fields: `routerProfile?`, `planModel?`, `editModel?`
- `parseArgs` accepts the three flags

Flag propagation to runtime router construction is part of the Tier 1 wiring packet (same follow-up as T-012). The flags parse and surface on `Args` now so the wiring packet can consume them without further CLI changes.

Acceptance R6 AC-2, AC-3 (flags present and parseable) met. AC-1 (fresh-install plain-CLI split) pending live wiring.
