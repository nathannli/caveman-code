Build site: context/plans/build-site-token-efficiency.md
# T-te-013: Router determinism — no time/randomness
**Status:** done

`DefaultModelRouter.route()` is a pure function: map lookup on `ctx.role` + `cavekitPhase`, no `Date.now()`, no `Math.random()`, no `process.env` reads.

Test: "is deterministic across 1000 identical calls" in router.test.ts asserts model/retention/profile equality across 1000 invocations.

Acceptance R4 AC-1, AC-2 met. AC-3 (cache prefix does not drift due to routing): satisfied because the decision is byte-stable given fixed context, so the cache prefix the assembler produces downstream also remains stable. Separate integration assertion belongs in Tier 1 wiring.
