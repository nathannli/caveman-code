# T-018: RTK Integration Settings
**Status:** done

Changed:
- `packages/coding-agent/src/core/settings-manager.ts`
- `packages/coding-agent/test/settings-manager.test.ts`

What changed:
- Fixed `RtkSettings.enabled` default annotation to `true` in `settings-manager.ts`
- Added tests covering:
  - `getRtkEnabled()` defaulting to `true`
  - `setRtkEnabled()` writing `rtk.enabled` to global settings
  - persistence across fresh `SettingsManager.create(...)` sessions
  - re-enabling after disable

Validation:
- Targeted test run:
  - `cd packages/coding-agent && node ../../node_modules/vitest/dist/cli.js --run test/settings-manager.test.ts`
  - blocked by existing unrelated runtime failure in `packages/coding-agent/src/config.ts`:
    - `ReferenceError: Cannot access 'ENV_PACKAGE_DIR' before initialization`
- Repo check run:
  - `npm run check`
  - blocked by existing unrelated type errors:
    - `packages/cavekit-extension/src/rtk-exec.ts`
    - `packages/coding-agent/test/rtk.test.ts`

Acceptance criteria:
- R3/AC-1 met
  - `rtk.enabled` exists in settings manager model
  - runtime default is `true` via `getRtkEnabled(): boolean { return this.settings.rtk?.enabled ?? true; }`
- R3/AC-4 met
  - `getRtkEnabled()` and `setRtkEnabled(enabled: boolean)` exist in API
- R3/AC-5 met
  - setter writes to global settings and persists across new manager instances
  - covered by added test
- R3/AC-2 not validated in this task
  - integration-level behavior deferred to T-020/T-021
- R3/AC-3 not validated in this task
  - integration-level behavior deferred to T-020/T-021

Notes:
- Required RTK settings API and persistence logic already existed in `settings-manager.ts`
- This task tightened correctness by aligning default annotation with runtime behavior and adding explicit coverage for default + persistence
