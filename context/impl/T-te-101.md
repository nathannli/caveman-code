Build site: context/plans/build-site-token-efficiency.md
# T-te-101: (file, hunks[]) diff payload for both new tools
**Status:** done
`packages/agent/src/tools/edit-tools-registry.ts` — `DiffPayload = { file, hunks: Hunk[] }`, `Hunk = { before, after, lineRange }`. `toDiffPayload()` builds a uniform payload; both `apply_sr_diff` and `edit_symbol` results map into this shape so the T-118 review UI consumes a single contract.
Test: "toDiffPayload builds uniform hunk shape".
Acceptance R5 AC-1, AC-2, AC-3 met.
